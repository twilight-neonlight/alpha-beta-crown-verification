# ─── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/

# ─── Virtual environments ────────────────────────────────
venv/
.venv/
env/

# ─── IDE / editor ────────────────────────────────────────
.vscode/
.idea/

# ─── OS files ────────────────────────────────────────────
.DS_Store
Thumbs.db

# ─── Misc ────────────────────────────────────────────────
.env
*.log
.ipynb_checkpoints/
.claude

# Prior assignment's report, kept locally only as a reference (not part of this submission).
report_marabou_reference.pdf

# WSL/conda setup scripts and the report generator — kept locally, not required deliverables.
scripts/
# Intermediate Word version of the report (the submitted deliverable is report.pdf).
report.docx

# ─── alpha-beta-CROWN: external tool, installed separately ─
# The cloned verifier lives outside this repo (WSL: ~/alpha-beta-CROWN).
alpha-beta-CROWN/
alpha-beta-crown/

# ─── alpha-beta-CROWN: generated artifacts (not source) ───
# Compiled VNNLIB cache the verifier writes next to each spec.
*.vnnlib.compiled
vnnlib/*.compiled
# Counterexamples / cached bounds / output dumps.
*.pkl
*.pt
*.npz
out.txt
# Binary pickle summary emitted by the run (we keep the readable .txt log instead).
results/abcrown_out.txt
